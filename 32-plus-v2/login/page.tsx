'use client';
import { FormEvent, useState } from 'react';
export default function Login(){const [mode,setMode]=useState<'tenant'|'owner'>('tenant'); const submit=(e:FormEvent)=>{e.preventDefault(); alert('The login system will be connected to the 32+ account database in the next build.');}; return <main className="simple-page"><header className="nav"><a href="/" className="logo-link"><img src="/logo.png" alt="32+" /></a><div className="nav-title">SEASONAL RENTALS</div><nav><a href="/">Find a home</a><a href="/owners">For owners</a></nav></header><section className="login-page"><div className="login-card"><p className="eyebrow">32+ ACCOUNT</p><h1>Welcome back.</h1><div className="login-tabs"><button className={mode==='tenant'?'active':''} onClick={()=>setMode('tenant')}>Tenant</button><button className={mode==='owner'?'active':''} onClick={()=>setMode('owner')}>Owner</button></div><form onSubmit={submit}><label>Email<input type="email" required placeholder="you@example.com" /></label><label>Password<input type="password" required placeholder="••••••••" /></label><button className="primary-button" type="submit">Sign in <span>→</span></button></form><p className="small-note">{mode==='tenant'?'Your tenant account will let you save homes and manage enquiries.':'Your owner account will let you manage properties, availability and enquiries.'}</p></div></section></main>}
