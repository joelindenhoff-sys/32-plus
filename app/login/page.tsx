'use client';
import {FormEvent,useState} from 'react';import {Suspense} from 'react';import {useSearchParams} from 'next/navigation';import {Header} from '../components';
function Box(){const q=useSearchParams();const[role,setRole]=useState(q.get('mode')==='owner'?'owner':'tenant');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');function submit(e:FormEvent){e.preventDefault();if(password.length<4)return setError('Password must be at least 4 characters.');localStorage.setItem('32plus_user',JSON.stringify({email,role}));location.href=role==='owner'?'/dashboard':'/homes'}return <div className="login-wrap"><div className="login-card"><p className="eyebrow">32+</p><h1>Sign in</h1><div className="tabs"><button className={role==='tenant'?'active':''} onClick={()=>setRole('tenant')}>Tenant</button><button className={role==='owner'?'active':''} onClick={()=>setRole('owner')}>Owner</button></div><form className="form" onSubmit={submit}><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="error">{error}</div>}<button className="primary">Continue →</button></form></div></div>}
export default function Login(){return <><Header/><Suspense fallback={<div className="login-wrap">Loading…</div>}><Box/></Suspense></>}
